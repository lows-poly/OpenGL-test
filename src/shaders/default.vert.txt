#version 330 core

layout ( location = 0 ) in vec3 aPos;
layout ( location = 1 ) in vec3 aColour;

out vec4 vertex_colour;
uniform float scale;

void main()
{
	gl_Position = vec4( aPos * scale, 1.0 );
	vertex_colour = vec4( aColour, 1.0 );
}
